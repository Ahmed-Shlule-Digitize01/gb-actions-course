name: 03 - Workflow Runners

on: push

jobs: 
    ubuntu-echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show OS
              run: |
                echo "This job is running on Ubuntu runner"
                echo "Runnur OS: $RUNNER_OS"